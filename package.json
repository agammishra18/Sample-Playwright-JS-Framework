{
  "name": "playwright-hybrid-sample-framework",
  "version": "1.0.0",
  "description": "Sample Playwright + TypeScript hybrid test framework (POM + Data-Driven + API) with Allure reporting and GitHub Actions CI.",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/ui",
    "test:api": "playwright test tests/api",
    "test:ddt": "playwright test tests/data-driven",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report:html": "playwright show-report",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "format:check": "tsc --noEmit"
  },
  "dependencies": {
    "@playwright/test": "^1.57.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@types/node": "^20.11.0",
    "allure-playwright": "^2.15.0",
    "typescript": "^5.4.0"
  }
}
